#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    const int size=20;
    char napis[size];
    int x=0;
    cin.get(napis, size);
    //cout<<strlen(napis);
    for(; napis[x]!='\0'; x++){
    }
    cout<<x;
    return 0;
}
