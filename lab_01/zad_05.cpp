#include <iostream>

using namespace std;

int main()
{
    int a, b;
    cout << "Podaj a: ";
    cin >> a;
    cout << "Podaj b: ";
    cin >> b;
    cout << "\nSuma: " << a + b << "\nIloczyn: " << a * b << "\nIloraz: " << a / b;

    return 0;
}
